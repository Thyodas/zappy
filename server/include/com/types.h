/*
** EPITECH PROJECT, 2023
** myteams
** File description:
** types.h
*/

#pragma once

#include <uuid/uuid.h>
#include <stdbool.h>
#include <stdint.h>
#include <string.h>
#include <time.h>

#define MAX(x, y) ((x > y) ? (x) : (y))
#define MIN(x, y) ((x < y) ? (x) : (y))
